class AlarmClock {
    constructor(alarmCollection, timerId){
        this.alarmCollection = [];
        this.timerId = timerId;
    }

    addClock(time, callback, id){
        if (id = undefined ){
            throw new Error('error text');
        }
        if(id === id){
            console.error();
            return false;
        }
        this.alarmCollection.push(addClock);
    }
}
